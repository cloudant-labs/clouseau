plugins {
    // id('com.cloudant.commons')
    id('scala')
}

task deps {
    doLast {
       configurations.findAll{it.canBeResolved}.each{
            println(it)
            it.resolve()
       }
    }
}
