package com.cloudant.ziose.clouseau

/**
 * This is a trait used by clouseau. We don't use it at the moment.
 * However we need it to be compatible with existing clouseau codebase.
 * The mixin trait for creating a class which is instrumented with metrics.
 */
trait Instrumented {}
