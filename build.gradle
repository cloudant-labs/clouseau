plugins {
  id 'org.scoverage' version "7.0.1" apply false
  id 'org.owasp.dependencycheck' version '7.1.2' apply false
  id "com.github.spotbugs" version "5.0.12" apply false
}

allprojects {
  project.ext.version = '1.0'
  project.ext.outputDir = file("${rootDir}/build/jar-files")
}
