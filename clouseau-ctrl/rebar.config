{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps, [clouseau_ctrl]}.
{escript_main_app, clouseau_ctrl}.
{escript_name, clouseau_ctrl}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]}
    ]}
]}.
